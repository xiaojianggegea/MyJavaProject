package com.ghj.servlet;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;import java.io.IOException;/** * 创建Session对象 *     getId() 获取Session的会话标识符 *     getCreationTime() 获取Session的创建时间 *     getLastAccessedTime() 获取Session最后启动时间 *     isNew() 判断是否是新的session对象 * */@WebServlet("/session01")public class Servlet01Session extends HttpServlet {    @Override    protected void service(HttpServletRequest req, HttpServletResponse resp) throws IOException {        //获取Session对象        HttpSession session=req.getSession();        //获取Session的会话标识符        String id=session.getId();        System.out.println("标识符："+id);        resp.setContentType("text/html;charset=utf-8");        resp.getWriter().write("标识符："+id);        //获取Session的创建时间        long time1=session.getCreationTime();        System.out.println("创建时间："+time1);        resp.getWriter().write("创建时间："+(int)time1);        //获取Session最后启动时间        long time2=session.getLastAccessedTime();        System.out.println("最后启动时间："+time2);        resp.getWriter().write("最后启动时间："+time2);        //判断是否是新的session对象        boolean TrueAndFalse=session.isNew();        System.out.println("是否为新对象："+TrueAndFalse);        resp.getWriter().write("是否为新对象：" + TrueAndFalse);    }}