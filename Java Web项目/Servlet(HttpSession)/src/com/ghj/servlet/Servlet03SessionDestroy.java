package com.ghj.servlet;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;/** * Session对象的销毁 * 1、默认时间销毁 *      Tomcat中的销毁时间为30min,即不操作界面的时间，一旦有操作，则重新计时 * 2、手动设置时间 *      getMaxInactiveInterval()查看当前Session对象的最大不活动时间 *      setMaxInactiveInterval(int) 修改Session对象的最大不活动时间 * 3、立即销毁 *       session.invalidate(); * 4、关闭浏览器失效 *       session底层依赖cookie，Cookie对象默认只在浏览器内存存中存活，关闭浏览器销毁 * 5、关闭服务器销毁 */@WebServlet("/session03")public class Servlet03SessionDestroy extends HttpServlet {    @Override    protected void service(HttpServletRequest req, HttpServletResponse resp) {        //获取Session对象        HttpSession session=req.getSession();        //设置Session域对象        session.setAttribute("uname","Admin");        session.setAttribute("pwd","123456");        /*//Session对象的最大不活动时间        int time=session.getMaxInactiveInterval();        System.out.println(time);        resp.getWriter().write(time);       //修改最大时间        session.setMaxInactiveInterval(15);//15秒*/        //立即销毁Session对象        session.invalidate();    }}