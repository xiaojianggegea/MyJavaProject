package com.ghj.servlet;import javax.servlet.ServletException;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.io.IOException;/** * 重定向与请求转发的区别 *     1、请求转发地址栏不发生变化，重定向地址栏发生变化 *     2、请求转发只有一次请求，重定向地址存在两次请求(效率) *     3、请求转发request对象共享，重定向request不共享 *     4、请求转发是服务端行为，重定向是客户端行为 *     5、请求转发的地址只能是该项目中的资源，重定向可以是任意资源 */@WebServlet("/ser")public class Difference extends HttpServlet {    @Override    protected void service(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {        //接受参数        String username=req.getParameter("uname");        System.out.println("uname:"+username);        resp.getWriter().write("uname"+username);        //设置reques域对象        req.setAttribute("pwd","123456");        //请求转发        //req.getRequestDispatcher("InformationPage.jsp").forward(req,resp);        //重定向        //resp.sendRedirect("InformationPage.jsp");        resp.sendRedirect("http://www.gsut.edu.cn/");    }}